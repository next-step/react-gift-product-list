import styled from '@emotion/styled';

export const StyledNavLoginBtn = styled.button`
  display: flex;
  align-items: center;
  padding: 12px 30px;
  border: none;
  background-color: ${({ theme }) => theme.sementicPalette.backgroundDefault};
`;
