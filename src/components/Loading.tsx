import styled from '@emotion/styled';

const Container = styled.div`
  width: 100%;
`;

const Loading = () => {
  return (
    <Container>
      <h1>Loading...</h1>
    </Container>
  );
};

export default Loading;
